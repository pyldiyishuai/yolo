# 视障人士环境感知与识别系统源程序清单说明

**版本**：V1.0  
**文档用途**：用于说明项目主要源程序目录、核心文件用途及模块关系，便于软著申报和源码材料整理。

---

## 1. 文档说明

本文件用于对系统的主要源程序目录和核心源码文件进行说明，帮助阅读者快速理解项目代码结构、功能分工和模块职责。本文档适合与源码压缩包、设计说明书、功能模块说明文档一并使用。

---

## 2. 项目源码总体结构

项目源码主要分为以下几个部分：

1. 根目录说明文件  
2. 前端源码目录  
3. 后端源码目录  
4. 部署配置目录  
5. 模型与日志目录  

---

## 3. 根目录主要文件说明

### 3.1 `README.md`
项目总说明文档，包含项目简介、目录结构、启动方式、文档索引和软著整理建议。

### 3.2 `功能模块说明文档.md`
用于说明系统功能模块划分及各模块作用。

### 3.3 `需求文档.md`
用于说明项目背景、使用场景、功能需求与验收标准。

### 3.4 `软件说明书.md`
用于说明软件概况、运行环境、功能结构和应用价值。

### 3.5 `软件操作手册.md`
用于说明系统启动方式、页面操作流程与功能使用步骤。

### 3.6 `软件设计说明书.md`
用于说明系统架构、模块设计、接口关系和数据存储方式。

### 3.7 `主要界面截图说明文档.md`
用于整理系统主要页面截图与界面说明。

### 3.8 `源程序清单说明.md`
用于说明主要源码目录和核心文件用途。

---

## 4. 前端源码目录说明

前端目录路径为：`frontend/`

### 4.1 `frontend/package.json`
前端项目依赖与脚本配置文件。

### 4.2 `frontend/vite.config.js`
Vite 构建配置文件，用于前端开发服务与接口代理配置。

### 4.3 `frontend/src/main.js`
前端入口文件，用于挂载 Vue 应用、路由和状态管理。

### 4.4 `frontend/src/App.vue`
应用根组件，负责全局布局、导航栏和统一界面结构。

### 4.5 `frontend/src/assets/main.css`
全局样式文件，负责主题颜色、布局风格和无障碍视觉规范。

### 4.6 `frontend/src/router/index.js`
路由配置文件，用于定义首页、监测页、设置页和求助页的页面跳转规则。

---

## 5. 前端状态管理文件说明

### 5.1 `frontend/src/stores/system.js`
用于管理系统运行状态、识别结果、风险提示和语音播报相关状态。

### 5.2 `frontend/src/stores/voice.js`
用于管理语音识别、唤醒词处理和指令解析逻辑。

### 5.3 `frontend/src/stores/settings.js`
用于管理语音参数、紧急联系人、常用路线和本地持久化设置。

---

## 6. 前端页面文件说明

### 6.1 `frontend/src/views/HomeView.vue`
首页界面文件，用于展示系统概况、状态和主要功能入口。

### 6.2 `frontend/src/views/MonitorView.vue`
环境监测页面文件，用于调用摄像头、展示识别结果、统计信息、权限状态和历史记录。

### 6.3 `frontend/src/views/SettingsView.vue`
设置页面文件，用于配置语音参数、紧急联系人、路线信息和查看系统信息。

### 6.4 `frontend/src/views/EmergencyView.vue`
应急求助页面文件，用于触发 SOS 求助、显示位置信息和查看求助记录。

---

## 7. 后端源码目录说明

后端目录路径为：`backend/`

### 7.1 `backend/main.py`
后端服务入口文件，用于创建 FastAPI 应用并注册各类业务接口。

### 7.2 `backend/requirements.txt`
后端依赖清单文件，用于安装运行系统所需的 Python 库。

---

## 8. 后端模块文件说明

### 8.1 `backend/modules/system.py`
用于提供系统启动、停止和状态查询接口。

### 8.2 `backend/modules/detect.py`
用于完成图像识别、目标解析、距离估算、方向判断和风险评估，是识别核心模块。

### 8.3 `backend/modules/data.py`
用于保存和读取识别日志、风险记录，并向前端提供历史查询和统计分析接口。

### 8.4 `backend/modules/emergency.py`
用于处理应急求助逻辑，包括求助消息生成、通知发送、日志记录和求助历史查询。

---

## 9. 后端工具与辅助文件说明

### 9.1 `backend/utils/location.py`
用于提供位置距离计算和路线相关辅助逻辑，为后续路线偏离检测功能预留基础能力。

### 9.2 `backend/models/`
用于存放目标识别模型文件。

### 9.3 `backend/logs/`
用于存放 SQLite 数据文件和求助日志文件。

---

## 10. 部署目录说明

部署目录路径为：`deploy/`

### 10.1 `deploy/config.ini`
用于保存系统统一配置项，例如端口、模型路径和短信服务配置。

### 10.2 `deploy/start.ps1`
Windows 环境下一键启动脚本。

### 10.3 `deploy/start.sh`
Linux 或 macOS 环境下一键启动脚本。

---

## 11. 核心源码职责汇总

| 文件路径 | 主要职责 |
|------|------|
| `frontend/src/views/HomeView.vue` | 首页展示与功能入口 |
| `frontend/src/views/MonitorView.vue` | 环境监测、识别结果、统计信息、历史查询 |
| `frontend/src/views/SettingsView.vue` | 参数设置、联系人管理、路线配置 |
| `frontend/src/views/EmergencyView.vue` | SOS 求助、定位信息、求助历史 |
| `frontend/src/stores/system.js` | 系统状态、风险提示、语音播报 |
| `frontend/src/stores/voice.js` | 语音识别与指令控制 |
| `frontend/src/stores/settings.js` | 本地设置持久化 |
| `backend/modules/detect.py` | 目标识别、距离方向判断、风险评估 |
| `backend/modules/data.py` | 日志存储、历史记录、统计分析 |
| `backend/modules/emergency.py` | 应急求助与历史记录管理 |
| `backend/modules/system.py` | 系统状态控制 |

---

## 12. 源程序整理建议

如需用于软著源码提交，建议：
- 保留前端和后端核心源码；
- 保留配置文件和说明文档；
- 不必将 `node_modules` 等第三方依赖目录作为重点说明对象；
- 将核心源码按模块分段整理，便于打印或导出。

---

## 13. 结论

本项目源码结构清晰，前后端模块划分明确，核心文件职责分工合理，能够较好体现系统在环境识别、风险预警、语音控制、应急求助和数据管理等方面的完整软件实现逻辑。通过本清单说明，可进一步提升软著源码材料的规范性与可读性。
