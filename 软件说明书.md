# 视障人士环境感知与识别系统软件说明书

**版本**：V1.0  
**文档用途**：用于软件著作权申报、项目资料归档、系统说明与答辩支撑材料编写。

---

## 1. 软件概述

### 1.1 软件名称
视障人士环境感知与识别系统

### 1.2 软件简介
本软件是一套面向视障人士日常出行与环境感知辅助场景设计的本地部署式辅助系统。系统通过摄像头采集周围环境图像，结合目标识别、风险评估、语音反馈与应急求助等功能，为用户提供环境感知、风险提醒和紧急处理支持。

系统采用前后端分离架构，前端提供无障碍交互界面与语音控制能力，后端负责目标识别、风险判断、日志记录和求助处理，整体具备较好的模块化程度和可扩展性。

### 1.3 软件目标
本软件的建设目标主要包括：
- 为视障人士提供环境目标识别能力；
- 为用户提供高、中、低风险分级提示；
- 为用户提供语音播报与语音指令控制能力；
- 在紧急情况下为用户提供一键求助能力；
- 为系统运行过程提供本地日志记录与统计分析能力；
- 为后续扩展导航、离线语音识别等能力预留接口结构。

---

## 2. 运行环境

### 2.1 硬件环境
- 普通个人计算机或笔记本设备
- 支持摄像头采集
- 支持麦克风输入
- 支持扬声器或耳机播放

### 2.2 软件环境
- 操作系统：Windows / Linux / macOS
- Python：3.10 及以上
- Node.js：建议 18 及以上
- 浏览器：Chrome、Edge 等支持现代 Web API 的浏览器

### 2.3 运行依赖
- 前端框架：Vue 3、Vite、Pinia、Vue Router、Axios
- 后端框架：FastAPI、Uvicorn
- 视觉识别：YOLOv8、OpenCV
- 数据存储：SQLite、本地存储 localStorage
- 语音能力：SpeechRecognition、SpeechSynthesis

---

## 3. 软件总体结构

本软件由前端表现层、后端服务层、识别处理层和数据存储层组成。

### 3.1 前端表现层
前端主要负责：
- 首页、监测页、设置页、求助页等界面展示；
- 摄像头、麦克风、定位等浏览器能力调用；
- 风险提示、语音播报、权限状态展示；
- 用户参数设置、联系人管理和操作流程承载。

### 3.2 后端服务层
后端主要负责：
- 提供系统状态控制接口；
- 接收图像并进行目标识别；
- 计算距离、方向与风险等级；
- 保存识别日志、风险日志和求助记录；
- 处理求助消息发送与历史记录查询。

### 3.3 数据存储层
系统采用本地轻量化存储方案：
- 前端配置数据通过 `localStorage` 保存；
- 后端日志数据通过 `SQLite` 保存；
- 求助记录可写入本地日志文件，便于事件留痕和查询。

---

## 4. 软件主要功能

### 4.1 系统首页功能
- 展示系统名称与运行状态；
- 提供主要功能入口；
- 显示系统运行概况；
- 作为用户进入系统后的统一导航界面。

### 4.2 环境监测功能
- 打开设备摄像头采集图像；
- 实时识别周围目标；
- 展示识别目标名称、方向、距离和风险等级；
- 支持手动识别与自动识别模式。

### 4.3 风险预警功能
- 根据识别结果进行风险评估；
- 采用高、中、低三级风险划分；
- 对危险目标进行语音播报；
- 对高风险状态进行明显提示。

### 4.4 语音交互功能
- 支持语音唤醒词识别；
- 支持启动、关闭、监测、设置、求助等语音指令；
- 支持语音播报反馈；
- 支持语速、音量和反馈模式配置。

### 4.5 应急求助功能
- 支持一键求助；
- 支持读取当前位置；
- 支持调用紧急联系人信息；
- 支持求助记录查看与状态反馈。

### 4.6 数据记录与统计功能
- 保存识别日志；
- 保存风险事件记录；
- 保存求助历史记录；
- 展示识别次数、风险次数等统计信息。

### 4.7 设置管理功能
- 配置语音播报参数；
- 管理紧急联系人；
- 配置常用路线；
- 保存本地化设置内容。

### 4.8 无障碍支持功能
- 提供高对比度界面；
- 支持大按钮和大字号；
- 支持 ARIA 语义化结构；
- 支持键盘操作与屏幕阅读器访问。

---

## 5. 软件特点

本软件具有以下特点：

1. **场景明确**：面向视障辅助领域，应用目标清晰。  
2. **功能完整**：识别、预警、语音、求助形成闭环。  
3. **本地部署**：无需复杂服务器即可运行。  
4. **模块清晰**：便于软著材料整理与后续扩展。  
5. **交互友好**：兼顾可视化展示与无障碍语音反馈。  
6. **数据留痕**：支持历史记录和统计分析。  

---

## 6. 软件适用对象

本软件主要适用于以下对象：
- 视障人士及弱视用户；
- 无障碍辅助系统研究人员；
- 毕业设计与项目答辩展示人员；
- 视障辅助类软件原型验证人员；
- 需要进行环境感知辅助功能研究的开发人员。

---

## 7. 软件应用价值

本软件在技术与应用上具有一定价值：
- 可用于视障辅助出行方向的软件设计与功能验证；
- 可作为无障碍交互系统研究案例；
- 可作为本地部署型智能识别系统原型；
- 可为后续导航辅助、离线识别、设备集成等扩展提供基础。

---

## 8. 结论

视障人士环境感知与识别系统是一套集环境识别、风险提示、语音交互、应急求助、设置管理与数据记录于一体的本地辅助软件系统。系统功能完整、模块清晰、结构合理，适合用于软件著作权申报、毕业设计材料整理及后续功能扩展开发。
