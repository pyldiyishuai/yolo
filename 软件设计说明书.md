# 视障人士环境感知与识别系统软件设计说明书

**版本**：V1.0  
**文档用途**：用于说明软件总体设计思路、系统结构、模块划分、数据组织与接口关系。

---

## 1. 文档说明

本说明书用于描述视障人士环境感知与识别系统的总体设计方案，包含系统架构、模块组成、数据存储方式、主要接口与处理流程等内容，可用于软件著作权申报、毕业设计文档编写和后续技术扩展参考。

---

## 2. 设计目标

本系统的设计目标包括：
- 构建一套前后端分离的本地部署式辅助软件系统；
- 实现环境识别、风险提示、语音交互与应急求助等核心能力；
- 采用轻量化存储结构，降低部署成本；
- 保持模块边界清晰，便于扩展与维护；
- 提供适合无障碍场景的交互方式。

---

## 3. 总体架构设计

系统总体上分为四层：

1. 表现层  
2. 业务逻辑层  
3. 识别处理层  
4. 数据存储层  

### 3.1 表现层
表现层由 Vue 3 前端构成，负责用户界面展示与交互，主要包含：首页、环境监测页、设置页和应急求助页。

### 3.2 业务逻辑层
业务逻辑层由前端状态管理与后端接口服务共同组成，负责运行控制、识别流程调用、求助流程处理、日志查询和设置持久化等功能。

### 3.3 识别处理层
识别处理层主要由 YOLO 模型推理逻辑与图像处理逻辑组成，用于完成图像识别、距离估算、方向判断和风险分级。

### 3.4 数据存储层
数据存储层主要使用 `localStorage` 与 `SQLite`：
- `localStorage` 用于保存前端设置；
- `SQLite` 用于保存识别日志和风险日志；
- 本地日志文件用于记录求助历史。

---

## 4. 前端设计

### 4.1 前端技术选型
- Vue 3
- Vite
- Pinia
- Vue Router
- Axios

### 4.2 前端页面设计

#### 4.2.1 首页
首页用于展示系统概况、运行状态和主要功能入口。

#### 4.2.2 环境监测页
环境监测页用于完成摄像头采集、识别结果展示、风险日志显示、历史记录展示、统计信息查看与权限状态查看。

#### 4.2.3 设置页
设置页用于调整语音参数、管理紧急联系人、配置常用路线并查看系统基础信息。

#### 4.2.4 应急求助页
应急求助页用于触发一键求助、获取定位、查看联系人状态和查询求助历史记录。

### 4.3 前端状态管理设计
系统前端主要通过状态管理模块实现数据共享：
- `system.js`：管理系统状态、识别结果、播报内容、风险提示；
- `voice.js`：管理语音识别、唤醒词与指令解析；
- `settings.js`：管理语音参数、联系人、路线及本地持久化。

---

## 5. 后端设计

### 5.1 后端技术选型
- Python 3.10+
- FastAPI
- Uvicorn
- OpenCV
- YOLOv8
- SQLite

### 5.2 后端模块设计

#### 5.2.1 `system.py`
负责系统运行状态控制与状态接口。

#### 5.2.2 `detect.py`
负责图像识别、结果解析、距离估算、方向判断与风险分级。

#### 5.2.3 `data.py`
负责识别日志、风险日志、统计分析和历史记录查询。

#### 5.2.4 `emergency.py`
负责求助消息生成、求助通知发送与求助历史记录管理。

#### 5.2.5 `location.py`
负责距离计算与路线辅助能力，为后续路线偏离检测提供支持。

---

## 6. 核心模块设计说明

### 6.1 环境识别模块设计
该模块接收前端上传图像，经过图像预处理后，调用 YOLO 模型完成目标检测，输出目标列表。每个目标包含类别、置信度、距离、方向、风险等级等信息。

### 6.2 风险评估模块设计
该模块根据目标类型、目标大小和相对位置对识别对象进行风险判断，将结果划分为高风险、中风险和低风险，并将结果返回前端进行展示与播报。

### 6.3 语音交互模块设计
该模块基于浏览器语音能力实现语音唤醒、指令解析和语音播报。通过前端状态模块实现对页面跳转、功能触发和语音反馈的联动控制。

### 6.4 应急求助模块设计
该模块接收用户点击 SOS 或语音求助触发的信息，读取联系人和位置信息，生成求助消息并调用短信发送逻辑或模拟发送逻辑，同时保存本地求助日志。

### 6.5 日志与统计模块设计
该模块采用 SQLite 存储识别与风险日志，通过查询接口向前端提供历史记录和统计数据，用于页面展示与后期数据分析。

---

## 7. 数据存储设计

### 7.1 前端本地存储设计
前端使用 `localStorage` 保存以下数据：
- 语速设置
- 音量设置
- 反馈模式
- 紧急联系人信息
- 常用路线信息

### 7.2 后端数据库设计
后端采用 SQLite 保存日志数据，主要包括以下表：

#### 7.2.1 识别日志表 `detection_log`
建议字段包括：
- `id`
- `ts`
- `label`
- `label_en`
- `risk`
- `distance`
- `direction`
- `confidence`

#### 7.2.2 风险记录表 `risk_record`
建议字段包括：
- `id`
- `ts`
- `level`
- `message`

### 7.3 求助记录存储设计
求助记录采用本地日志文件方式存储，主要字段包括：
- 求助时间
- 联系人姓名
- 联系电话
- 位置信息
- 风险信息
- 发送状态

---

## 8. 主要接口设计

### 8.1 系统状态接口
- 启动系统接口
- 停止系统接口
- 查询系统状态接口

### 8.2 识别接口
- 图像检测接口 `/api/detect`

### 8.3 日志接口
- 写入识别日志接口 `/api/log/detection`
- 读取识别日志接口 `/api/log/detection`
- 历史识别查询接口 `/api/log/detection/history`
- 写入风险日志接口 `/api/log/risk`
- 风险日志查询接口 `/api/log/risk`
- 数据统计接口 `/api/log/stats`

### 8.4 求助接口
- 发送求助接口 `/api/emergency`
- 求助历史查询接口 `/api/emergency/history`

---

## 9. 主要处理流程设计

### 9.1 环境监测流程
1. 用户进入监测页面；
2. 前端申请摄像头权限；
3. 用户开启摄像头；
4. 前端采集图像帧；
5. 图像发送至后端识别接口；
6. 后端返回识别结果；
7. 前端展示识别信息并执行风险提示；
8. 同时保存识别日志与风险日志。

### 9.2 语音控制流程
1. 用户授予麦克风权限；
2. 系统开始语音监听；
3. 识别唤醒词与指令内容；
4. 将语音结果映射为操作命令；
5. 执行页面跳转或功能调用；
6. 使用 TTS 进行结果反馈。

### 9.3 求助处理流程
1. 用户点击 SOS 按钮或发出求助指令；
2. 系统读取联系人与位置信息；
3. 生成求助消息；
4. 调用短信服务或模拟通知逻辑；
5. 返回发送结果；
6. 保存求助历史记录。

---

## 10. 安全与异常处理设计

### 10.1 权限处理
系统对摄像头、麦克风、定位权限进行状态检测，并在权限异常时给出提示。

### 10.2 异常降级
当模型未正确加载或部分依赖不可用时，系统可进入演示模式，以便继续进行界面演示和功能验证。

### 10.3 日志留痕
系统会保存识别日志、风险日志和求助记录，便于问题定位和运行回溯。

---

## 11. 可扩展设计

系统在设计上预留以下扩展方向：
- 自训练模型替换与扩展；
- 离线语音识别接入；
- 路线偏离检测能力；
- 多终端协同能力；
- 地图导航与云端同步能力。

---

## 12. 结论

本系统采用前后端分离与轻量本地存储方案，整体结构清晰、模块划分明确，能够较好支撑环境识别、风险预警、语音交互、求助处理与日志分析等功能需求。系统设计兼顾了无障碍交互、部署轻量化和后续扩展需求，适合用于软件著作权申报和项目技术资料整理。
