# 视障人士环境感知与识别系统软件操作手册

**版本**：V1.0  
**文档用途**：用于说明系统安装、启动、页面操作流程及主要功能使用方法。

---

## 1. 手册说明

本手册用于指导用户完成系统启动、页面进入、环境监测、参数设置、应急求助和记录查看等操作，适用于项目演示、答辩、系统测试及软著相关说明材料整理。

---

## 2. 运行前准备

在使用本系统前，请确认具备以下条件：
- 已安装 Python 3.10 及以上版本；
- 已安装 Node.js 环境；
- 当前设备支持摄像头、麦克风和扬声器；
- 使用 Chrome 或 Edge 等支持浏览器权限调用的浏览器；
- 已根据项目目录安装前后端依赖。

---

## 3. 系统启动方法

### 3.1 启动后端服务
在项目根目录下打开终端，执行：

```powershell
cd backend
python -m pip install -r requirements.txt
python -m uvicorn main:app --host 127.0.0.1 --port 8000
```

### 3.2 启动前端服务
另开一个终端窗口，执行：

```powershell
cd frontend
npm install
npm run dev
```

### 3.3 打开系统界面
启动成功后，在浏览器访问：

- 前端地址：`http://localhost:5173`
- 后端接口文档：`http://127.0.0.1:8000/docs`

---

## 4. 系统页面说明

系统主要包含以下页面：

1. 首页  
2. 环境监测页  
3. 设置页  
4. 应急求助页  

用户可通过导航栏进入不同页面进行操作。

---

## 5. 首页操作说明

首页主要用于展示系统概况和功能入口。

### 5.1 首页主要功能
- 查看系统名称与简介；
- 查看系统当前运行状态；
- 进入环境监测页面；
- 进入设置页面；
- 进入应急求助页面；
- 快速启动或停止系统。

### 5.2 首页操作步骤
1. 打开系统首页；
2. 查看系统状态；
3. 点击对应入口按钮进入目标功能页；
4. 如需开始使用监测能力，可先启动系统。

---

## 6. 环境监测页操作说明

环境监测页是系统核心操作页面，用于打开摄像头并查看识别结果。

### 6.1 打开摄像头
1. 进入“环境监测”页面；
2. 点击“开启摄像头”按钮；
3. 浏览器弹出权限请求时，选择允许；
4. 摄像头画面显示成功后，系统进入采集状态。

### 6.2 进行环境识别
摄像头开启后，系统会进行目标识别，并在页面上展示：
- 目标名称；
- 风险等级；
- 距离信息；
- 方向信息；
- 识别数量和实时日志。

### 6.3 查看统计信息
在环境监测页中，还可查看：
- 总识别次数；
- 风险事件数量；
- 高风险次数；
- 常见识别目标；
- 权限状态；
- 历史识别记录；
- 风险事件筛选结果。

### 6.4 停止采集
如需停止识别：
1. 点击“停止采集”或关闭摄像头按钮；
2. 系统停止采集与识别；
3. 页面状态切换为未采集状态。

---

## 7. 设置页操作说明

设置页用于配置语音参数、紧急联系人和常用路线。

### 7.1 配置语音参数
可进行以下设置：
- 调整语速；
- 调整音量；
- 选择反馈模式；
- 点击“测试语音”验证效果。

### 7.2 管理紧急联系人
可进行以下操作：
- 添加联系人姓名与手机号；
- 删除联系人；
- 查看当前联系人列表。

### 7.3 管理常用路线
可进行以下操作：
- 新增起点与终点；
- 保存常用路线；
- 删除不再使用的路线。

### 7.4 检测后端连接
设置页可查看：
- 系统版本；
- 使用的识别引擎；
- 前后端框架；
- 当前后端连接状态。

---

## 8. 应急求助页操作说明

应急求助页用于在紧急情况下快速发送求助信息。

### 8.1 获取当前位置
进入求助页后，系统会尝试获取当前位置。用户也可点击“刷新位置”按钮重新获取定位信息。

### 8.2 发起应急求助
1. 确保已配置紧急联系人；
2. 进入“应急求助”页面；
3. 点击中心 SOS 按钮；
4. 系统自动生成求助消息；
5. 系统尝试发送至紧急联系人；
6. 页面显示发送结果。

### 8.3 查看求助记录
在求助页面底部可查看历史求助记录，包括：
- 求助时间；
- 位置摘要；
- 求助状态；
- 联系人信息。

---

## 9. 语音操作说明

系统支持语音唤醒与语音指令控制。

### 9.1 语音使用前提
- 浏览器支持语音识别；
- 已允许麦克风权限；
- 当前系统语音功能处于可用状态。

### 9.2 常用语音指令示例
- 小视 启动
- 小视 关闭
- 小视 监测
- 小视 设置
- 小视 求助
- 小视 回家

### 9.3 语音反馈说明
当用户发出指令后，系统会通过语音播报提示执行结果，例如：
- 系统已启动；
- 摄像头已开启；
- 已添加联系人；
- 求助已发送。

---

## 10. 异常情况处理

### 10.1 摄像头无法打开
可能原因：
- 浏览器未授予摄像头权限；
- 摄像头被其他程序占用；
- 当前设备无摄像头。

处理方法：
- 检查浏览器权限设置；
- 关闭占用摄像头的软件；
- 更换设备或摄像头硬件。

### 10.2 麦克风无法使用
可能原因：
- 浏览器未授予麦克风权限；
- 当前浏览器不支持语音识别。

处理方法：
- 重新授权麦克风；
- 更换支持的浏览器。

### 10.3 无法获取定位
可能原因：
- 浏览器未授予定位权限；
- 当前网络或系统定位服务不可用。

处理方法：
- 重新授权定位权限；
- 刷新页面后重试。

### 10.4 后端未连接
可能原因：
- 后端服务未启动；
- 前端代理配置异常；
- 端口被占用。

处理方法：
- 重新启动后端服务；
- 检查端口与配置；
- 查看接口文档是否可访问。

---

## 11. 关闭系统

使用完成后，可按以下方式关闭：
1. 关闭摄像头采集；
2. 停止前端页面使用；
3. 在终端中停止前后端服务；
4. 关闭浏览器页面。

---

## 12. 附加说明

本系统适合用于：
- 软著申报材料整理；
- 毕业设计功能演示；
- 无障碍辅助软件原型展示；
- 目标识别与语音交互系统研究。
